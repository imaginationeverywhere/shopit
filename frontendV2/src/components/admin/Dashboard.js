import React from 'react';
import Layout from '../admin'
export default function Dashboard() {
    return (
        <Layout>
            <section>
                DASHBOARD ADMIN
            </section>
        </Layout>
    );
}